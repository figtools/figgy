Basic tests to validate it's working.... mostly...

figgy config sync --env dev --config test/ci-config.json
figgy config sync --env dev --config test/data_repl_conf.json --replication-only --prompt

